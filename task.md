# Current Task Context

## 今回やること・目的 (Goal/Objective)
- 既存コードの挙動を全て棚卸しし、Next.js + TypeScript 移行のための詳細要件を確定する
- **現行デザインを一切変更しない**ことを前提に、画面/導線/挙動/外部依存を明文化する

## やること (Must)
- 既存 Flask 実装の全ルート/ページ/JS 挙動/外部依存を洗い出して要件化する
- 認証（/login → /bingo）とセッション条件を明確化する
- `/map` と `/event` の iframe 連携（`window.spots` / `window.map`）要件を明記する
- `static/` のパス互換（`/static/...` と `./static/...`）を維持する前提で要件を書く
- **UI/HTML/CSS/画像/アニメーションは変更しない**ことを要件に含める

## やらないこと (Non-goals)
- 実際の移行作業（Next.js 実装・TypeScript 化）は行わない
- UI/レイアウト/文言/色/アニメーションの変更は行わない
- ルーティングの追加・削除（未使用テンプレートの新規公開含む）は行わない
- Dockerfile / `docker-compose.yml` の作成や動作検証は行わない

## 受け入れ基準 (Acceptance Criteria)
- `spec.md` に全ルート/画面/挙動/外部依存/環境変数/静的パス互換が明記されている
- `task.md` に目的/やること/やらないこと/受け入れ基準/影響範囲が具体的に記載されている
- **現行デザインを一切変更しない**という制約が明文化されている

## 影響範囲 (Impact/Scope)

- **触るファイル**:
- `spec.md`
- `task.md`

- **壊しちゃいけない挙動**:
- 既存の画面表示（レイアウト/色/画像/アニメーション/タイポグラフィ）
- 既存の URL/遷移/iframe 連携/外部リンク
- 認証/リダイレクト/LocalStorage を含む既存のクライアント挙動
