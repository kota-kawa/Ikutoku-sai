services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
      SECRET_KEY: ${SECRET_KEY}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
